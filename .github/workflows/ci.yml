name: CI

on:
  push:
    branches: [main, develop, 'feature/**']
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements/*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements/train.txt
          pip install -r requirements/serve.txt
          # ruff and pytest are pinned so CI and local runs agree; an
          # unpinned ruff silently changed its default rule set and broke CI.
          pip install pytest==9.1.1 ruff==0.16.4 pillow numpy

      - name: Lint with ruff
        run: ruff check src/ tests/

      - name: Verify the serving app imports
        run: python -c "import sys; sys.path.insert(0, 'src'); import serve; print(serve.app.title)"

      - name: Run unit tests
        run: pytest tests/ -v --tb=short

  build-docker-train:
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build training image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.train
          push: false
          tags: mlops-train:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-docker-serve:
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build serving image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.serve
          push: false
          tags: mlops-serve:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
